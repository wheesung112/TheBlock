Dependencies:

-This package requires the Visual Effects graph, make sure it's installed on your project, otherwise 
the effects won't work.

-With the visual effects graph installed, go to Edit -> Preferences -> Visual Effects -> check the 
'Experimental Operators/Blocks' option.
